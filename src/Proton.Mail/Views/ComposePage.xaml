<Page
    x:Class="Proton.Mail.Views.ComposePage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid Padding="16" RowSpacing="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="New message" Style="{StaticResource TitleTextBlockStyle}" />

        <TextBox Grid.Row="1" Header="To" PlaceholderText="recipient@example.com" Text="{x:Bind ViewModel.To, Mode=TwoWay}" />

        <TextBox Grid.Row="2" Header="Subject" Text="{x:Bind ViewModel.Subject, Mode=TwoWay}" />

        <TextBox
            Grid.Row="3"
            Header="Message"
            AcceptsReturn="True"
            TextWrapping="Wrap"
            Text="{x:Bind ViewModel.Body, Mode=TwoWay}" />

        <StackPanel Grid.Row="4" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
            <InfoBar
                IsOpen="{x:Bind ViewModel.IsSent, Mode=OneWay}"
                Severity="Success"
                Message="Message queued to send."
                IsClosable="False" />
            <Button Content="Discard" Command="{x:Bind ViewModel.DiscardCommand}" />
            <Button Content="Send" Style="{StaticResource AccentButtonStyle}" Command="{x:Bind ViewModel.SendCommand}" />
        </StackPanel>
    </Grid>
</Page>
